<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>contactdone.ejs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 20px;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            background-color: #99ff99;
            padding: 15px;
            font-weight: bold;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-radius: 8px;
        }
        .description {
            background-color: #f0f8ff;
            padding: 15px;
            border-radius: 8px;
            margin-top: 10px;
        }
        .contact-footer {
            text-align: right;
            margin-top: 15px;
            font-size: 14px;
            color: #555;
        }
        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        .btn-close {
            background-color: #ccc;
        }
        .contact-image {
            max-width: 100%;
            height: auto;
            display: block;
            margin-top: 10px;
            border-radius: 5px;
        }

        .btn {
            transition: background-color 0.3s, transform 0.2s;
        }

        .btn:hover {
            opacity: 0.8;
            transform: scale(1.05);
        }

        .btn-close:hover {
            background-color: #b3b3b3;
        }

        .btn-accept:hover {
            background-color: #5cb85c;
        }


    </style>
</head>
<body style="background-color: #DAE7E7;">
    <div class="container">
        <div class="header">
            <span>หอพัก <%= contact.dormitory_name %> ห้อง <%= contact.room_id %></span>
            <span><%= new Date(contact.date).toLocaleString('th-TH') %></span>
        </div>
        <div class="description">
            <h3>เรื่อง: <%= contact.topic %></h3>
            <p><%= contact.description %></p>
            <% if (contact.picture) { %>
                <img src="<%= contact.picture %>" alt="Contact Image" class="contact-image">
            <% } %>
            
            <br>
            <% if (contact.response) { %>
                <p><strong>คำตอบ:</strong> <%= contact.response %></p>
                <p><strong>ตอบกลับเมื่อ:</strong> <%= new Date(contact.response_date).toLocaleString('th-TH') %></p>
            <% } %>
        </div>
        
        <div class="contact-footer">
            <p><%= contact.firstName %> <%= contact.lastName %>, <%= contact.telephone %></p>
        </div>

        <button class="btn btn-close" onclick="window.history.back()">ปิด</button>
    </div>
</body>
</html>